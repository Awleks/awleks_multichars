Config = Config or {}

Config.Theme = {
    accent = '#2563EB', -- buttons, badges, slider, fade corners
    surface = '#0E0E10', -- cards, inputs, dropdowns
    fade = '#111111', -- center overlay
    fadeOpacity = 0.80, -- overlay strength (0.0 - 1.0)
    danger = '#F87171', -- validation errors
    textMuted = '#9CA3AF', -- secondary labels
}
